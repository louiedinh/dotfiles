if (interactive()) {
  suppressMessages(require(devtools))
}

geo_mean <- function(x) {exp(mean(log(x)))}

# use-this
options(
  usethis.full_name = "Louie Dinh",
  usethis.description = list(
    `Authors@R` = 'person(
        "Louie", "Dinh",
        email = "LouieDinh@gmail.com",
        role = c("aut", "cre")
     )'
  )
)

# blogdown
options(blogdown.author="Louie Dinh",
        blogdown.ext=".Rmd")

# Set print to 3 digits by default
options(digits = 3)

source("~/Lib/Rlib/utils.R")
